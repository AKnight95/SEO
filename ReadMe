Critère utilisé pour le bon référencement:
    -Balise titre complété et cohérent 
    -Balise Alt des images complété
    -Balise Meta description compélté
    -Balise Meta viewport intégré et complété
    -Balise Meta robots intégré et complété
    -Balise Meta Og Social Media intégré et complété
    -Balise Meta Canonical intégré et complété
    -Utilisation cohérente des balise de titrage h1, h2, h3 et h4.
    -Utilisation de balise "SEO friendly" adapté (balise section, header...)

Calcul du taux de clic à la fin du trimestre:

    Taux de clic = (Nombre de clics / nombres d’impression) * 100
    -> (2400 / 5000) * 100 = 48%

Calcul du taux de clic, taux de conversion et taux d'acquisition après être passé en mode payant:

    Taux de clic = (Nombre de clics / nombres d’impression) * 100
    -> (7000 / 8000) * 100 = 87.5%.

    Taux de conversion = (Nombres de concrétisation / nombre de clic) * 100
    -> (5000 / 7000) * 100 = 71.4285714286 %.

    Taux  d'acquisition = ( coût par clic * nombre de clic) / nombre de concrétisations
    -> (1.5 * 7000) / 5000 = 2.1 euros par contrat.